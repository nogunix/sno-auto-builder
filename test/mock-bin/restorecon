#!/usr/bin/env bash
# Mock `restorecon` for test/mock-run.sh. Silence means "nothing relabelled",
# which is what the playbook's changed_when keys off.
set -u
echo "[mock-restorecon] $*" >> "${MOCK_LOG:-/dev/null}"
exit 0
