coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto

ignore:
  - "test/**"
  - "test-render.yml"
  - ".github/**"
  - "callback_plugins/**"
